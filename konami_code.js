const code = [38, 38, 40, 40, 37, 39, 37, 39, 66, 65]
let index = 0;
function init(e) {
  // Write your JavaScript code inside the init() function
}

var listener = document.body;
listener.addEventListener('click', function init(e) {
  // Write your JavaScript code inside the init() function
  
  
  if (key.which == code[index]) {
    index++; }
 
    if (index == code.length) {
      alert("something");
 
      index = 0;
  } else {
    index = 0;
  }

});